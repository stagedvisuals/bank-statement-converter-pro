# BSC PRO Onboarding Agent Report
**Date:** March 2, 2026  
**Time:** 19:02 UTC  
**Agent:** Onboarding Retention Flow

## Summary
Agent triggered to check for users stuck at 20% onboarding progress for 24+ hours.

## Database Query
```sql
SELECT user_id, progress_percentage, started_at 
FROM onboarding_status 
WHERE progress_percentage = 20 
  AND started_at < NOW() - INTERVAL '24 hours'
  AND completed_at IS NULL;
```

## Actions Performed
1. ✅ Checked onboarding_status table configuration
2. ✅ Verified API endpoints exist (/api/agents/onboarding/run, /api/agents/onboarding/send-retention-email)
3. ✅ Confirmed retention email template is ready
4. ⚠️ CRON_SECRET not available in local environment - agent requires manual trigger with auth

## Email Template
Subject: "💡 Je bent er bijna! Nog een paar stappen..."

Content:
- Acknowledge their progress (20%)
- Remaining steps: upload factuur, download export, receive +2 credits
- CTA button: "Ga verder waar je gebleven was"
- Support contact for help

## API Endpoints Status
| Endpoint | Status |
|----------|--------|
| /api/agents/onboarding/run | ✅ Exists |
| /api/agents/onboarding/send-retention-email | ✅ Exists |

## Next Run
Scheduled: Daily at 19:02 UTC

## SQL for Retention Flow Table (if not exists)
```sql
CREATE TABLE IF NOT EXISTS onboarding_retention_flows (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
    current_step TEXT NOT NULL,
    trigger_reason TEXT,
    email_sent BOOLEAN DEFAULT false,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

## Recommendation
Set CRON_SECRET environment variable in Vercel dashboard to enable automated execution.

---
*Generated by BSC PRO Onboarding Agent*
